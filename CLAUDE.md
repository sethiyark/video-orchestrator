# CLAUDE.md

Claude Code reads this file, not `AGENTS.md`. Import the shared project guide so
Cursor, Codex, and Claude Code stay on one source of truth. Add Claude-only
notes below the import if needed.

@AGENTS.md

Path-scoped copies of [`.agents/rules/`](.agents/rules/) are linked from
[`.claude/rules/`](.claude/rules/) (`docs.md` always; `python.md` / `frontend.md`
when matching `paths:`). Do not duplicate those files here.
